<?xml version="1.0" encoding="UTF-8"?>
<!--build_version=3.4-->
<!-- 
This build script was generated by Codename One to build native mobile applications using Java.
To learn more about Codename One go to https://www.codenameone.com/
-->

<project name="CN1-Deploy" default="default" basedir=".">
    <description>Builds, tests, and runs the project WebBrowserDemo.</description>
    <import file="build.xml"/>
    
    <target name="build-for-javascript-sync" depends="clean,copy-javascript-override,copy-libs,jar,clean-override">
        <codeNameOne 
            jarFile="${dist.jar}"
            displayName="${codename1.displayName}"
            packageName = "${codename1.packageName}"
            mainClassName = "${codename1.mainName}"
            version="${codename1.version}"
            icon="${codename1.icon}"
            vendor="${codename1.vendor}"
            subtitle="${codename1.secondaryTitle}"
            automated="true"
            targetType="javascript"
            />
        
    </target>
    
    <target name="build-for-mac-os-x-desktop-sync" depends="clean,copy-desktop-override,copy-libs,jar,clean-override">
        <codeNameOne 
            jarFile="${dist.jar}"
            displayName="${codename1.displayName}"
            packageName = "${codename1.packageName}"
            mainClassName = "${codename1.mainName}"
            version="${codename1.version}"
            icon="${codename1.icon}"
            vendor="${codename1.vendor}"
            subtitle="${codename1.secondaryTitle}"
            automated="true"
            targetType="desktop_macosx"
            includeSource="true"
            />
        <delete>
            <fileset dir="${build.classes.dir}" includes="**/*.class"/>
        </delete>
    </target>
</project>
